k8sServiceHost: "100.78.153.26"
k8sServicePort: 6443

operator:
  replicas: 1

gatewayAPI:
  enabled: true

hubble:
  enabled: true
  relay:
    enabled: true
  ui:
    enabled: true

ipam:
  mode: "kubernetes"

l2announcements:
  enabled: true

kubeProxyReplacement: true

k8sClientRateLimit:
  qps: 50
  burst: 100

MTU: 1200 # to fit tailscale mtu of 1280

encryption:
  enabled: false # let tailscale handle the security


routingMode: native
autoDirectNodeRoutes: false
ipv4NativeRoutingCIDR: "10.42.0.0/16"

devices: "tailscale0"
directRoutingDevice: "tailscale0"

loadBalancer:
  acceleration: disabled

bpf:
  masquerade: true
  tproxy: true

k8s:
  requireIPv4PodCIDR: true
